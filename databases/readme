Create table danger_type (
    id int NOT NULL PRIMARY KEY,
    name varchar(50) NOT NULL
);

Create table danger_count (
    id int NOT NULL PRIMARY KEY,
    danger_type_id int NOT NULL,
    CONSTRAINT fk_danger_count_type FOREIGN KEY (danger_type_id) REFERENCES danger_type (id)
);
